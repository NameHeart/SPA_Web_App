export interface Person {
  id: string;
  prefix: string;
  firstName: string;
  lastName: string;
  birthdate: string;
  nationality: string;
  citizenId: string;
  gender: string;
  phoneNumber: string;
  passport: string;
  expectedSalary: number;
}
